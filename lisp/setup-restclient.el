;;; setup-restclient.el  ---  restclient config
;;; Commentary:
;;; restclient mode related config
;;; Code:

(add-to-list 'auto-mode-alist '("\\.http$" . restclient-mode))

(provide 'setup-restclient)
;;; setup-restclient.el ends here
